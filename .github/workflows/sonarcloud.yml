name: "SonarCloud Analysis for Flutter"

on:
  push:
    branches: [main]
  pull_request:
    branches: ["*"]

jobs:
  sonarcloud:
    name: SonarCloud Scan
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend  # <-- Your Flutter project folder

    steps:
      # 1️⃣ Checkout repo
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2️⃣ Set up JDK 17 (required by SonarCloud)
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      # 3️⃣ Cache SonarCloud packages
      - name: Cache SonarCloud packages
        uses: actions/cache@v3
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar

      # 4️⃣ Install Flutter (updated for Dart 3.10.7+)
      - name: Install Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.4'  # <-- Fixes Dart SDK version issue

      # 5️⃣ Install dependencies
      - name: Install dependencies
        run: flutter pub get

      # 6️⃣ Optional: Analyze code
      - name: Flutter Analyze
        run: flutter analyze

      # 7️⃣ Optional: Build project
      - name: Flutter Build
        run: flutter build apk --debug

      # 8️⃣ Run SonarCloud analysis
      - name: Run SonarCloud Scanner
        uses: SonarSource/sonarcloud-github-action@v2
        with:
          projectBaseDir: .
          extraProperties: |
            sonar.projectKey=YOUR_PROJECT_KEY
            sonar.organization=YOUR_ORG_KEY
            sonar.sources=lib
            sonar.exclusions=**/build/**, **/*.g.dart
            sonar.c.file.suffixes=-
            sonar.cpp.file.suffixes=-
            sonar.objc.file.suffixes=-
